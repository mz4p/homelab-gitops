apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nginx-route
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`nginx.local`)
      kind: Rule
      services:
        - name: nginx
          port: 80
