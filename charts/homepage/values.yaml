homepage:
  service:
    main:
      type: ClusterIP
      ports:
        http:
          port: 3000

  config:
    title: "K3s Homelab"
    headerStyle: clean

    services:
      - Infrastructure:
        - ArgoCD:
            icon: argocd.png
            href: "https://argocd.local"
            description: GitOps Controller
