---
kube-prometheus-stack:
  grafana:
    admin:
      existingSecret: grafana-credentials
      userKey: admin-user
      passwordKey: admin-password
    ingress:
      enabled: false
  kubeProxy:
    enabled: false
  alertmanager:
    enabled: false
