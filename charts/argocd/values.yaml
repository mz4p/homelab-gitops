---
argo-cd:
  configs:
    params:
      server.insecure: true
    cm:
      account.admin: apiKey, login

  server:
    extraArgs:
      - --insecure
    
    host: argocd.local
    
    ingress:
      enabled: false
